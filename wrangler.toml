name = "oriz-home"
compatibility_date = "2026-06-17"
compatibility_flags = ["nodejs_compat"]

# Cloudflare Pages: static build output dir (Astro → dist).
pages_build_output_dir = "./dist"

# Custom domain bound via dashboard:
#   oriz.in   -> oriz-home
